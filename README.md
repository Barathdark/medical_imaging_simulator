I built a healthcare imaging performance testing platform in Python that simulates PACS workflows. The system generates DICOM files, simulates imaging devices sending scans concurrently using multithreading, and measures system performance metrics such as latency and throughput using pandas and numpy. A PyQt dashboard allows users to run load simulations and export performance reports to Excel. The platform also simulates HL7 patient messages and supports launching external imaging tools using Python subprocess.